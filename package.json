{
  "name": "hubspot-firebase-sync",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "start": "firebase emulators:start",
    "build": "tsc",
    "deploy:dev": "firebase deploy --only functions --project domoblock-dev",
    "deploy:prod": "firebase deploy --only functions --project domoblock-app",
    "dev": "npm run build && npm start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@hubspot/api-client": "^12.0.1",
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "firebase-admin": "^13.0.2",
    "firebase-functions": "^6.3.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.10",
    "dotenv": "^16.4.7",
    "typescript": "^5.7.3"
  }
}
